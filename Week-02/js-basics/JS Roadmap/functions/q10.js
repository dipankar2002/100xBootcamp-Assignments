// Question:
// Write two functions, and then compose them into a third function. For example,
// if f(x) = x + 2 and g(x) = 2x, then the composed function should be
// h(x) = f(g(x)).

// Input:
// { x: 5 }

// Output:
// Result of the composed function.

// Approach Hint:
// Call one function and pass its result into the other function.

function solve(input) {
    // TODO: write your logic here
}

// Example Test Cases:
console.log(solve({ x: 5 }));

