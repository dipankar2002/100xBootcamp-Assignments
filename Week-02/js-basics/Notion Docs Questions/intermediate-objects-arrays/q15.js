// Question:
// Find intersection of all arrays in object.
// Given an object where each value is an array, return values that appear in
// every array.

// Input:
// { a: [1, 2, 3], b: [2, 3, 4], c: [3, 4, 5] }

// Output:
// [3]

// Approach Hint:
// Start with one array and keep only values found in all other arrays.

function solve(input) {
    // TODO: write your logic here
}

// Example Test Cases:
console.log(solve({ a: [1, 2, 3], b: [2, 3, 4], c: [3, 4, 5] }));

