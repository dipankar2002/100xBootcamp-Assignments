// Question:
// Deep flatten nested arrays inside object.
// Given an object where values are nested arrays, return a new object where
// each array is fully flattened.

// Input:
// { a: [1, [2, [3]]], b: [4, [5]] }

// Output:
// { a: [1, 2, 3], b: [4, 5] }

// Approach Hint:
// Flatten each array value, including arrays inside arrays.

function solve(input) {
    // TODO: write your logic here
}

// Example Test Cases:
console.log(solve({ a: [1, [2, [3]]], b: [4, [5]] }));

