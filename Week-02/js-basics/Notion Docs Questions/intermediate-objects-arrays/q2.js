// Question:
// Transform API response to object (id to name).
// Given an array of user objects, return an object where each id maps to the
// matching name.

// Input:
// [
//   { id: 1, name: "Alice" },
//   { id: 2, name: "Bob" }
// ]

// Output:
// { 1: "Alice", 2: "Bob" }

// Approach Hint:
// Use each id as a key and store the name as its value.

function solve(input) {
    // TODO: write your logic here
}

// Example Test Cases:
console.log(solve([
    { id: 1, name: "Alice" },
    { id: 2, name: "Bob" }
]));

