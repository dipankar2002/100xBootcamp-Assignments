// Question:
// Sum values in object arrays.
// Given an object where each key has an array of numbers, return a new object
// where each key contains the sum of its array values.

// Input:
// { food: [10, 20, 30], travel: [5, 15], bills: [40, 60] }

// Output:
// { food: 60, travel: 20, bills: 100 }

// Approach Hint:
// Iterate through each key and add the numbers inside its array.

function solve(input) {
    // TODO: write your logic here
}

// Example Test Cases:
console.log(solve({ food: [10, 20, 30], travel: [5, 15], bills: [40, 60] }));
