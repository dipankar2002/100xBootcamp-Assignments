// Question:
// Pick only given keys from object.
// Given an object and an array of keys, return a new object with only those
// selected keys.

// Input:
// object = { name: "Rahul", age: 23, city: "Noida" }
// keys = ["name", "city"]

// Output:
// { name: "Rahul", city: "Noida" }

// Approach Hint:
// Loop through the keys array and copy matching values from the object.

function solve(input) {
    // TODO: write your logic here
}

// Example Test Cases:
console.log(solve({
    object: { name: "Rahul", age: 23, city: "Noida" },
    keys: ["name", "city"]
}));

