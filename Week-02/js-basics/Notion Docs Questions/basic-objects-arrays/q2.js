// Question:
// Count word occurrences in array.
// Given an array of words, return an object where each word stores how many
// times it appears.

// Input:
// ["apple", "banana", "apple", "orange", "banana", "apple"]

// Output:
// { apple: 3, banana: 2, orange: 1 }

// Approach Hint:
// Use an object to store counts while looping through the array.

function solve(input) {
    // TODO: write your logic here
}

// Example Test Cases:
console.log(solve(["apple", "banana", "apple", "orange", "banana", "apple"]));

