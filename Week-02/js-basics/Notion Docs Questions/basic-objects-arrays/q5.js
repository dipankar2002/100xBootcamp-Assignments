// Question:
// Flatten object of arrays into one array.
// Given an object where each key contains an array, return one combined array
// with all values.

// Input:
// { fruits: ["apple", "banana"], veggies: ["carrot", "pea"] }

// Output:
// ["apple", "banana", "carrot", "pea"]

// Approach Hint:
// Collect values from all arrays into a single result array.

function solve(input) {
    // TODO: write your logic here
}

// Example Test Cases:
console.log(solve({ fruits: ["apple", "banana"], veggies: ["carrot", "pea"] }));

