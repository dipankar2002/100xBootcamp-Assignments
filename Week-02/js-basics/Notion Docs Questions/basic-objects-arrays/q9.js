// Question:
// Unique values across all object arrays.
// Given an object where each key contains an array, return one array containing
// only unique values from all arrays.

// Input:
// { x: [1, 2, 3], y: [2, 3, 4], z: [4, 5] }

// Output:
// [1, 2, 3, 4, 5]

// Approach Hint:
// Combine all arrays, then keep only values that appear once in the result.

function solve(input) {
    // TODO: write your logic here
}

// Example Test Cases:
console.log(solve({ x: [1, 2, 3], y: [2, 3, 4], z: [4, 5] }));

